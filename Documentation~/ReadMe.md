# Mandala SDK

## Description

This section should provide a brief overview of what the package does and its intended use within Unity-based XR projects. Highlight any unique features or capabilities that distinguish your package from others.

## Features

- List the key features of your package.
- Mention any specific Unity or XR functionalities it enhances or simplifies.

## Installation

### Prerequisites

- Unity [version]
- Any other dependencies

### Steps

1. Explain how to install the package via NPM.
    ```bash
    npm install your-package-name --save
    ```

2. Additional steps, if necessary, to integrate the package into a Unity project.

## Usage

Provide a basic code sample that shows how to use the package within a Unity project. This should be a simple example that illustrates the package's primary functionality.

```csharp
// Example usage of the package
using YourPackageName;
public class ExampleUsage : MonoBehaviour
{
    void Start()
    {
        // Demonstrate package functionality
    }
}
